services:
  suno-service:
    network_mode: host
    ports: !reset []
    networks: !reset []

  speaki-service:
    network_mode: host
    ports: !reset []
    networks: !reset []

  judge-service:
    network_mode: host
    ports: !reset []
    networks: !reset []
